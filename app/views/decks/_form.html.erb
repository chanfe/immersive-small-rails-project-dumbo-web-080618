<%= form_for @deck do |f| %>
  <%= f.label :name %>
  <%= f.text_field :name %>
  <br>
  <%= f.collection_select :user_id, User.all, :id, :username %>
  <br>

  <%= f.collection_check_boxes :card_ids, Card.all, :id, :name %>
 <%= f.submit %>
<% end %>
