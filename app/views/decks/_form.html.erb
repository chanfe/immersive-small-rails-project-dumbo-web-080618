<%= form_for @deck do |f| %>
  <%= f.label :name %>
  <%= f.text_field :name %>
  <br>
  <%= f.collection_select :user_id, User.all, :id, :username %>
  <br>

  <!-- <% f.collection_check_boxes :card_ids, Card.all, :id, :name %> -->
  <% Card.all.each do |card| %>
    <%= label_tag "#{card.name}" %>
    <%= number_field :card_ids, card.id[:num] %>
    <br>
  <% end %>
 <%= f.submit %>
<% end %>
