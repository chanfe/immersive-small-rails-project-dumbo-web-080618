
<div class="styleguide-container mdl-layout form_stuff">
  <h1 class="styleguide-title title">Cards Index</h1>
  <% @cards.each do |card| %>
    <h3><%= link_to card.name , card_path(card) %></h3>
  <% end %>
  
</div>